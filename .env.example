# FocusFirewall Environment Configuration

# Optional: Real Jev API Key (leave empty to use explicit MOCK demo mode)
JEV_API_KEY=

# Optional: Jev API Endpoint
JEV_API_URL=https://api.jev.ai/v1/decide

# Optional: Default engine mode ('jev' or 'mock')
ENGINE_MODE=mock
